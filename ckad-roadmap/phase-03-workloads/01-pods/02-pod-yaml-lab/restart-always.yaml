apiVersion: v1
kind: Pod
metadata:
  name: restart-always
spec:
  containers:
  - args:
    - "sleep 5; echo Always; echo $?"
    command: ["bin/sh", "-c"]
    image: busybox
    name: worker
  restartPolicy: Always
