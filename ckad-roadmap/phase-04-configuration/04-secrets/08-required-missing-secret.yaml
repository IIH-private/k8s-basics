apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: required-missing-secret
  name: required-missing-secret
spec:
  volumes:
    - name: secret-volume
      secret:
        secretName: does-not-exist
  containers:
  - command:
    - sleep
    - "3600"
    image: alpine
    name: required-missing-secret
    volumeMounts:
      - name: secret-volume
        mountPath: /etc/secrets
        readOnly: true  
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
