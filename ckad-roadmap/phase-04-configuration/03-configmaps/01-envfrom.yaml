apiVersion: v1
kind: Pod
metadata:
  name: envfrom-demo
spec:
  containers:
  - command:
    - sh
    - -c
    - |
       echo "APP_COLOR=$APP_COLOR"
       echo "APP_MODE=$APP_MODE"
       sleep 3600
    envFrom:
    - configMapRef:
        name: app-config
    image: busybox:1.36
    name: demo
