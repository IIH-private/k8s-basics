apiVersion: v1
kind: Pod
metadata:
  name: request-only
spec:
  containers:
    - name: nginx
      image: nginx:1.29
      resources:
        requests:
          cpu: "200m"
          memory: "64Mi"
