apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx # MUST match Deployment labels
  ports:
    - protocol: TCP
      port: 80 # Service port    
      targetPort: 80 # Pod port
      nodePort: 30007
  type: NodePort    