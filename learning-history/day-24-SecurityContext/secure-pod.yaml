apiVersion: v1
kind: Pod
metadata:
  name: secure-pod
spec:
  securityContext:
    runAsUser: 1000
    runAsNonRoot: true  
  containers:
  - image: nginx
    name: nginx
    securityContext:
      readOnlyRootFilesystem: true  
