apiVersion: v1
kind: Pod
metadata:
  name: pod-day16
spec:
  containers:
    - name: app
      image: busybox
      command: ["sh", "-c", "echo CKAD-DAY16 >> /data/output.txt && sleep 3600"]
      volumeMounts:
        - name: storage
          mountPath: /data
  volumes:
    - name: storage
      persistentVolumeClaim:
       claimName: pvc-day16  
